# Patrol backend env example
DEBUG=false

POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=patrol
POSTGRES_PASSWORD=CHANGE_ME
POSTGRES_DB=patrol

# openssl rand -hex 32
JWT_SECRET=CHANGE_ME
ACCESS_TOKEN_MINUTES=30
REFRESH_TOKEN_DAYS=30

CORS_ORIGINS=["http://localhost:5173"]
